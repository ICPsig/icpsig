import { ReactElement } from 'react';
interface Props {
    message?: string;
    width?: number;
    waitMessage?: string;
    className?: string;
}
declare function FailedTransactionLottie({ message, width, waitMessage }: Props): ReactElement;
export default FailedTransactionLottie;
