import { ReactElement } from 'react';
interface Props {
    message?: string;
    width?: number;
    waitMessage?: string;
}
declare function SuccessTransactionLottie({ message, width, waitMessage }: Props): ReactElement;
export default SuccessTransactionLottie;
