import React from 'react';
declare const RemoveAddress: ({ addressToRemove, name, shared }: {
    addressToRemove: string;
    name: string;
    shared?: boolean | undefined;
}) => React.JSX.Element;
export default RemoveAddress;
