import { FC, PropsWithChildren } from 'react';
interface IContentWrapper extends PropsWithChildren {
    className?: string;
}
declare const ContentWrapper: FC<IContentWrapper>;
export default ContentWrapper;
